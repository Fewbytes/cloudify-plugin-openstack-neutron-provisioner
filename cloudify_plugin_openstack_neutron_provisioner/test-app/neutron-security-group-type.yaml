# vim: ts=4 sw=4 et
imports:
    - 'cloudify.types'

types:
    neutron_security_group:
        derived_from: cloudify.types.base
        properties:
            - security_group
        interfaces:
            cloudify.interfaces.lifecycle:
                - create: cloudify.plugins.cloudify_plugin_openstack_neutron_provisioner.security_group.create

